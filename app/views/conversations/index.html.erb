<h1>Conversations</h1>
<ul>
    <% @conversations.each do |conversation| %>
      <li>
        <a href="/conversations/<%= conversation.id %>"><%= conversation.title %></a>
      </li>
    <% end %>
</ul>